// Hermes - Content Script
// Runs on all web pages to enable content extraction

console.log('Hermes content script loaded');

// Content script is primarily used for security context
// Most extraction is done via executeScript for better control
// This file can be extended for additional features like:
// - Page annotations
// - Highlighting indexed content
// - Visual indicators that a page is indexed
// - Auto-indexing on page load (if enabled in settings)
